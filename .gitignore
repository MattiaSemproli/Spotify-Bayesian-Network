# Reproducible byproducts of running the notebook — already embedded in the
# notebook's own cell outputs, and regenerated automatically on re-run.
# Keep network_diagram.png tracked (main.tex depends on it to compile).
query*.png
markov_blanket_*.png
structure_learning_comparison.png
structure_comparison.png
processed_spotify*.csv
 
# LaTeX build artifacts
*.aux
*.log
*.bbl
*.blg
*.out
*.synctex.gz
 
# Jupyter
.ipynb_checkpoints/
 
# Python / venv
__pycache__/
*.pyc
faikr-venv/
*.egg-info/
 
# OS junk
.DS_Store
Thumbs.db

# Folder to save files at different point during development. 
# Useful to restore a file to a previous state.
checkpoints/